query PrimeBalance($walletAddress: String!) {
  primeBalance(walletAddress: $walletAddress) {
    cursor
    items {
      balance
      firstHeldTimestamp
      shiftedBalance
      tokenId
      walletId
    }
  }
}
