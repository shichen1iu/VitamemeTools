query PrimeHolders($cursor: String) {
  primeHolders(cursor: $cursor) {
    count
    cursor
    items {
      balance
      firstHeldTimestamp
      shiftedBalance
      tokenId
      walletId
    }
  }
}
